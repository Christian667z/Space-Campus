<UserControl x:Class="AstaLauncher.Views.GeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="White">
    <Grid>
        <StackPanel Margin="30">
            <TextBlock Text="🔑 Générateur de Clés (C# Natif)" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>
            
            <TextBlock Text="Nom de l'étudiant :" Foreground="#ffcccc" FontWeight="Bold" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="InputNom" Height="40" Background="#130000" Foreground="White" Padding="10" Margin="0,0,0,20" FontSize="16" BorderBrush="#ff2222"/>
            
            <TextBlock Text="HWID de l'étudiant :" Foreground="#ffcccc" FontWeight="Bold" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="InputHwid" Height="40" Background="#130000" Foreground="White" Padding="10" Margin="0,0,0,30" FontSize="16" BorderBrush="#ff2222"/>
            
            <Button Content="⚡ Générer &amp; Copier Message WhatsApp" Height="50" Background="#cc0000" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0" Click="Generate_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Border Background="#0d0000" BorderBrush="#440000" BorderThickness="1" CornerRadius="10" Margin="0,30,0,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="Clé Application (Base) :" Foreground="#883333" Margin="0,0,0,5" FontSize="14"/>
                    <TextBox x:Name="OutputBaseKey" IsReadOnly="True" Height="40" Background="Black" Foreground="#00ff41" FontWeight="Bold" FontSize="18" Padding="10" Margin="0,0,0,20" BorderThickness="0"/>
                    
                    <TextBlock Text="Clé Module Hacking :" Foreground="#883333" Margin="0,0,0,5" FontSize="14"/>
                    <TextBox x:Name="OutputHackKey" IsReadOnly="True" Height="40" Background="Black" Foreground="#ff9900" FontWeight="Bold" FontSize="18" Padding="10" BorderThickness="0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
