<UserControl x:Class="AstaAcademieLauncher.Views.AdminUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10">
            <!-- Login / Token -->
            <TextBox x:Name="TxtLoginUser" Width="160" Margin="0,0,8,0" />
            <PasswordBox x:Name="PwdLogin" Width="160" Margin="0,0,8,0" />
            <Button Content="Se connecter" Click="BtnLogin_Click" Margin="0,0,8,0"/>
            <Button Content="Déconnexion" Click="BtnLogout_Click" Margin="0,0,8,0"/>
            <!-- Add user -->
            <TextBox x:Name="TxtNewUser" Width="140" Margin="12,0,8,0"/>
            <PasswordBox x:Name="PwdNew" Width="140" Margin="0,0,8,0"/>
            <CheckBox x:Name="ChkAdmin" Content="Admin" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button Content="Ajouter" Click="BtnAdd_Click"/>
        </StackPanel>
        <ListView x:Name="LvUsers" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="#" Width="40" DisplayMemberBinding="{Binding id}"/>
                    <GridViewColumn Header="Username" Width="180" DisplayMemberBinding="{Binding username}"/>
                    <GridViewColumn Header="Admin" Width="80" DisplayMemberBinding="{Binding is_admin}"/>
                    <GridViewColumn Header="Created" Width="180" DisplayMemberBinding="{Binding created_at}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
