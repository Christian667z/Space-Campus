<Application x:Class="AstaAcademieApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Palette de couleurs Elegante (Bleu Nuit / Émeraude) -->
        <Color x:Key="BgDark">#030712</Color>
        <Color x:Key="BgCard">#0e1e3a</Color>
        <Color x:Key="BgSidebar">#0c1628</Color>
        <Color x:Key="AccentColor">#3b82f6</Color>
        <Color x:Key="PrimaryColor">#10b981</Color>
        
        <SolidColorBrush x:Key="BrushBgDark" Color="{StaticResource BgDark}"/>
        <SolidColorBrush x:Key="BrushBgCard" Color="{StaticResource BgCard}"/>
        <SolidColorBrush x:Key="BrushBgSidebar" Color="{StaticResource BgSidebar}"/>
        <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="BrushTextWhite" Color="#e2e8f0"/>
        <SolidColorBrush x:Key="BrushTextMuted" Color="#7099c0"/>
        <SolidColorBrush x:Key="BrushBorder" Color="#1e3a5f"/>
        
        <!-- Glow Effects (Subtils) -->
        <DropShadowEffect x:Key="NeonGlow" Color="{StaticResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
        <DropShadowEffect x:Key="PrimaryGlow" Color="{StaticResource PrimaryColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
        
        <!-- Style des boutons de Navigation (Sidebar) -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="6" Margin="10,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#132548"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1e3a5f"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Glassmorphism Card Style -->
        <Style x:Key="GlassCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#0affffff"/> <!-- Ultra léger fond blanc -->
            <Setter Property="BorderBrush" Value="#1affffff"/> <!-- Bordure ultra légère -->
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="10" Color="Black" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
